<!DOCTYPE html>

<html>
    <head>
        <meta charset="utf-8">
        <title>Page Title</title>
        <link rel="stylesheet" type="text/css" href="css/main.css">
        <script src="../js/main.js"></script>
    </head>

    <body>
        <header>
        </header>

        <main>
            <form action="/newNoteResult" method="post">
                <label>Box Code</label><br>
                <% if (locals.body) {%>
                    <input type="text" name="boxCode" id="boxCode" value="<%= body.boxCode %>" autofocus><br>
                <% } else { %>
                    <input type="text" name="boxCode" id="boxCode" autofocus><br>
                <% } %>

                <label>Your Note</label><br>
                <% if (locals.body) {%>
                    <input type="textarea" name="message" id="note" value="<%= body.message %>" autofocus><br>
                <% } else { %>
                    <input type="textarea" name="message" id="note" autofocus><br>
                <% } %>

                <input type="submit" value="Submit Note">

                <% include ../parts/errors %>
            </form>

            <a href="/">Back</a>
        </main>

        <footer>
        </footer>
    </body>
</html>