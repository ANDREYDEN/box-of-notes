import { render } from "@testing-library/react";
import React from "react";
import BoxCreate from "./BoxCreate";

describe('BoxCreate', () => {
    it.todo('creates a new box', () => {
        const { getByText } = render(<BoxCreate />);
    })
})